Variables are only available for nadam_32 (final) and nadam_temp (intermediate changed frequently during training)
EfficientNet_B4, vgg19 and ResNet50 weights are too large to be pushed onto the git repository
